import React from 'react'

const DetailInfo = (props) => {
    console.log(props.location.query.cardDetail)
    return (
        <div>
            Detail Individual
        </div>
    )
}

export default DetailInfo;
